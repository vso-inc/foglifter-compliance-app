/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { DatasourceDeviceService } from "../datasourceDevice.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { DatasourceDeviceCreateInput } from "./DatasourceDeviceCreateInput";
import { DatasourceDevice } from "./DatasourceDevice";
import { DatasourceDeviceFindManyArgs } from "./DatasourceDeviceFindManyArgs";
import { DatasourceDeviceWhereUniqueInput } from "./DatasourceDeviceWhereUniqueInput";
import { DatasourceDeviceUpdateInput } from "./DatasourceDeviceUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class DatasourceDeviceControllerBase {
  constructor(
    protected readonly service: DatasourceDeviceService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: DatasourceDevice })
  @nestAccessControl.UseRoles({
    resource: "DatasourceDevice",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createDatasourceDevice(
    @common.Body() data: DatasourceDeviceCreateInput
  ): Promise<DatasourceDevice> {
    return await this.service.createDatasourceDevice({
      data: {
        ...data,

        asset: data.asset
          ? {
              connect: data.asset,
            }
          : undefined,
      },
      select: {
        asset: {
          select: {
            id: true,
          },
        },

        created: true,
        datasourceId: true,
        id: true,
        isDeleted: true,
        isLocked: true,
        lastReportSent: true,
        lastReportTime: true,
        name: true,
        reportNumber: true,
        updated: true,
        updatedBy: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [DatasourceDevice] })
  @ApiNestedQuery(DatasourceDeviceFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "DatasourceDevice",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async datasourceDevices(
    @common.Req() request: Request
  ): Promise<DatasourceDevice[]> {
    const args = plainToClass(DatasourceDeviceFindManyArgs, request.query);
    return this.service.datasourceDevices({
      ...args,
      select: {
        asset: {
          select: {
            id: true,
          },
        },

        created: true,
        datasourceId: true,
        id: true,
        isDeleted: true,
        isLocked: true,
        lastReportSent: true,
        lastReportTime: true,
        name: true,
        reportNumber: true,
        updated: true,
        updatedBy: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: DatasourceDevice })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "DatasourceDevice",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async datasourceDevice(
    @common.Param() params: DatasourceDeviceWhereUniqueInput
  ): Promise<DatasourceDevice | null> {
    const result = await this.service.datasourceDevice({
      where: params,
      select: {
        asset: {
          select: {
            id: true,
          },
        },

        created: true,
        datasourceId: true,
        id: true,
        isDeleted: true,
        isLocked: true,
        lastReportSent: true,
        lastReportTime: true,
        name: true,
        reportNumber: true,
        updated: true,
        updatedBy: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: DatasourceDevice })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "DatasourceDevice",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateDatasourceDevice(
    @common.Param() params: DatasourceDeviceWhereUniqueInput,
    @common.Body() data: DatasourceDeviceUpdateInput
  ): Promise<DatasourceDevice | null> {
    try {
      return await this.service.updateDatasourceDevice({
        where: params,
        data: {
          ...data,

          asset: data.asset
            ? {
                connect: data.asset,
              }
            : undefined,
        },
        select: {
          asset: {
            select: {
              id: true,
            },
          },

          created: true,
          datasourceId: true,
          id: true,
          isDeleted: true,
          isLocked: true,
          lastReportSent: true,
          lastReportTime: true,
          name: true,
          reportNumber: true,
          updated: true,
          updatedBy: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: DatasourceDevice })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "DatasourceDevice",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteDatasourceDevice(
    @common.Param() params: DatasourceDeviceWhereUniqueInput
  ): Promise<DatasourceDevice | null> {
    try {
      return await this.service.deleteDatasourceDevice({
        where: params,
        select: {
          asset: {
            select: {
              id: true,
            },
          },

          created: true,
          datasourceId: true,
          id: true,
          isDeleted: true,
          isLocked: true,
          lastReportSent: true,
          lastReportTime: true,
          name: true,
          reportNumber: true,
          updated: true,
          updatedBy: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
