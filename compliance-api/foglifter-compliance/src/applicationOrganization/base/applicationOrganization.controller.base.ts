/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { ApplicationOrganizationService } from "../applicationOrganization.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { ApplicationOrganizationCreateInput } from "./ApplicationOrganizationCreateInput";
import { ApplicationOrganization } from "./ApplicationOrganization";
import { ApplicationOrganizationFindManyArgs } from "./ApplicationOrganizationFindManyArgs";
import { ApplicationOrganizationWhereUniqueInput } from "./ApplicationOrganizationWhereUniqueInput";
import { ApplicationOrganizationUpdateInput } from "./ApplicationOrganizationUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class ApplicationOrganizationControllerBase {
  constructor(
    protected readonly service: ApplicationOrganizationService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: ApplicationOrganization })
  @nestAccessControl.UseRoles({
    resource: "ApplicationOrganization",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createApplicationOrganization(
    @common.Body() data: ApplicationOrganizationCreateInput
  ): Promise<ApplicationOrganization> {
    return await this.service.createApplicationOrganization({
      data: {
        ...data,

        application: {
          connect: data.application,
        },
      },
      select: {
        application: {
          select: {
            id: true,
          },
        },

        created: true,
        id: true,
        level: true,
        name: true,
        typeField: true,
        updated: true,
        updatedBy: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [ApplicationOrganization] })
  @ApiNestedQuery(ApplicationOrganizationFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "ApplicationOrganization",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async applicationOrganizations(
    @common.Req() request: Request
  ): Promise<ApplicationOrganization[]> {
    const args = plainToClass(
      ApplicationOrganizationFindManyArgs,
      request.query
    );
    return this.service.applicationOrganizations({
      ...args,
      select: {
        application: {
          select: {
            id: true,
          },
        },

        created: true,
        id: true,
        level: true,
        name: true,
        typeField: true,
        updated: true,
        updatedBy: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: ApplicationOrganization })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "ApplicationOrganization",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async applicationOrganization(
    @common.Param() params: ApplicationOrganizationWhereUniqueInput
  ): Promise<ApplicationOrganization | null> {
    const result = await this.service.applicationOrganization({
      where: params,
      select: {
        application: {
          select: {
            id: true,
          },
        },

        created: true,
        id: true,
        level: true,
        name: true,
        typeField: true,
        updated: true,
        updatedBy: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: ApplicationOrganization })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "ApplicationOrganization",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateApplicationOrganization(
    @common.Param() params: ApplicationOrganizationWhereUniqueInput,
    @common.Body() data: ApplicationOrganizationUpdateInput
  ): Promise<ApplicationOrganization | null> {
    try {
      return await this.service.updateApplicationOrganization({
        where: params,
        data: {
          ...data,

          application: {
            connect: data.application,
          },
        },
        select: {
          application: {
            select: {
              id: true,
            },
          },

          created: true,
          id: true,
          level: true,
          name: true,
          typeField: true,
          updated: true,
          updatedBy: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: ApplicationOrganization })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "ApplicationOrganization",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteApplicationOrganization(
    @common.Param() params: ApplicationOrganizationWhereUniqueInput
  ): Promise<ApplicationOrganization | null> {
    try {
      return await this.service.deleteApplicationOrganization({
        where: params,
        select: {
          application: {
            select: {
              id: true,
            },
          },

          created: true,
          id: true,
          level: true,
          name: true,
          typeField: true,
          updated: true,
          updatedBy: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
