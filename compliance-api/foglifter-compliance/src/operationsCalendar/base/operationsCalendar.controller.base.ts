/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { OperationsCalendarService } from "../operationsCalendar.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { OperationsCalendarCreateInput } from "./OperationsCalendarCreateInput";
import { OperationsCalendar } from "./OperationsCalendar";
import { OperationsCalendarFindManyArgs } from "./OperationsCalendarFindManyArgs";
import { OperationsCalendarWhereUniqueInput } from "./OperationsCalendarWhereUniqueInput";
import { OperationsCalendarUpdateInput } from "./OperationsCalendarUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class OperationsCalendarControllerBase {
  constructor(
    protected readonly service: OperationsCalendarService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: OperationsCalendar })
  @nestAccessControl.UseRoles({
    resource: "OperationsCalendar",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createOperationsCalendar(
    @common.Body() data: OperationsCalendarCreateInput
  ): Promise<OperationsCalendar> {
    return await this.service.createOperationsCalendar({
      data: {
        ...data,

        asset: data.asset
          ? {
              connect: data.asset,
            }
          : undefined,

        contact: data.contact
          ? {
              connect: data.contact,
            }
          : undefined,

        operationsCalendarStatus: data.operationsCalendarStatus
          ? {
              connect: data.operationsCalendarStatus,
            }
          : undefined,
      },
      select: {
        asset: {
          select: {
            id: true,
          },
        },

        changeCoordinator: true,
        changeManager: true,
        changeRequest: true,

        contact: {
          select: {
            id: true,
          },
        },

        created: true,
        developmentManager: true,
        id: true,
        lastPatched: true,
        locked: true,
        lockedBy: true,
        note: true,

        operationsCalendarStatus: {
          select: {
            id: true,
          },
        },

        projectManager: true,
        reasonCode: true,
        scheduled: true,
        scheduledBy: true,
        serviceId: true,
        technicalLead: true,
        updated: true,
        updatedBy: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [OperationsCalendar] })
  @ApiNestedQuery(OperationsCalendarFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "OperationsCalendar",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async operationsCalendars(
    @common.Req() request: Request
  ): Promise<OperationsCalendar[]> {
    const args = plainToClass(OperationsCalendarFindManyArgs, request.query);
    return this.service.operationsCalendars({
      ...args,
      select: {
        asset: {
          select: {
            id: true,
          },
        },

        changeCoordinator: true,
        changeManager: true,
        changeRequest: true,

        contact: {
          select: {
            id: true,
          },
        },

        created: true,
        developmentManager: true,
        id: true,
        lastPatched: true,
        locked: true,
        lockedBy: true,
        note: true,

        operationsCalendarStatus: {
          select: {
            id: true,
          },
        },

        projectManager: true,
        reasonCode: true,
        scheduled: true,
        scheduledBy: true,
        serviceId: true,
        technicalLead: true,
        updated: true,
        updatedBy: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: OperationsCalendar })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "OperationsCalendar",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async operationsCalendar(
    @common.Param() params: OperationsCalendarWhereUniqueInput
  ): Promise<OperationsCalendar | null> {
    const result = await this.service.operationsCalendar({
      where: params,
      select: {
        asset: {
          select: {
            id: true,
          },
        },

        changeCoordinator: true,
        changeManager: true,
        changeRequest: true,

        contact: {
          select: {
            id: true,
          },
        },

        created: true,
        developmentManager: true,
        id: true,
        lastPatched: true,
        locked: true,
        lockedBy: true,
        note: true,

        operationsCalendarStatus: {
          select: {
            id: true,
          },
        },

        projectManager: true,
        reasonCode: true,
        scheduled: true,
        scheduledBy: true,
        serviceId: true,
        technicalLead: true,
        updated: true,
        updatedBy: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: OperationsCalendar })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "OperationsCalendar",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateOperationsCalendar(
    @common.Param() params: OperationsCalendarWhereUniqueInput,
    @common.Body() data: OperationsCalendarUpdateInput
  ): Promise<OperationsCalendar | null> {
    try {
      return await this.service.updateOperationsCalendar({
        where: params,
        data: {
          ...data,

          asset: data.asset
            ? {
                connect: data.asset,
              }
            : undefined,

          contact: data.contact
            ? {
                connect: data.contact,
              }
            : undefined,

          operationsCalendarStatus: data.operationsCalendarStatus
            ? {
                connect: data.operationsCalendarStatus,
              }
            : undefined,
        },
        select: {
          asset: {
            select: {
              id: true,
            },
          },

          changeCoordinator: true,
          changeManager: true,
          changeRequest: true,

          contact: {
            select: {
              id: true,
            },
          },

          created: true,
          developmentManager: true,
          id: true,
          lastPatched: true,
          locked: true,
          lockedBy: true,
          note: true,

          operationsCalendarStatus: {
            select: {
              id: true,
            },
          },

          projectManager: true,
          reasonCode: true,
          scheduled: true,
          scheduledBy: true,
          serviceId: true,
          technicalLead: true,
          updated: true,
          updatedBy: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: OperationsCalendar })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "OperationsCalendar",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteOperationsCalendar(
    @common.Param() params: OperationsCalendarWhereUniqueInput
  ): Promise<OperationsCalendar | null> {
    try {
      return await this.service.deleteOperationsCalendar({
        where: params,
        select: {
          asset: {
            select: {
              id: true,
            },
          },

          changeCoordinator: true,
          changeManager: true,
          changeRequest: true,

          contact: {
            select: {
              id: true,
            },
          },

          created: true,
          developmentManager: true,
          id: true,
          lastPatched: true,
          locked: true,
          lockedBy: true,
          note: true,

          operationsCalendarStatus: {
            select: {
              id: true,
            },
          },

          projectManager: true,
          reasonCode: true,
          scheduled: true,
          scheduledBy: true,
          serviceId: true,
          technicalLead: true,
          updated: true,
          updatedBy: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
