/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { SubnetService } from "../subnet.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { SubnetCreateInput } from "./SubnetCreateInput";
import { Subnet } from "./Subnet";
import { SubnetFindManyArgs } from "./SubnetFindManyArgs";
import { SubnetWhereUniqueInput } from "./SubnetWhereUniqueInput";
import { SubnetUpdateInput } from "./SubnetUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class SubnetControllerBase {
  constructor(
    protected readonly service: SubnetService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: Subnet })
  @nestAccessControl.UseRoles({
    resource: "Subnet",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createSubnet(@common.Body() data: SubnetCreateInput): Promise<Subnet> {
    return await this.service.createSubnet({
      data: data,
      select: {
        created: true,
        id: true,
        isActive: true,
        note: true,
        purpose: true,
        updated: true,
        updatedBy: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [Subnet] })
  @ApiNestedQuery(SubnetFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "Subnet",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async subnets(@common.Req() request: Request): Promise<Subnet[]> {
    const args = plainToClass(SubnetFindManyArgs, request.query);
    return this.service.subnets({
      ...args,
      select: {
        created: true,
        id: true,
        isActive: true,
        note: true,
        purpose: true,
        updated: true,
        updatedBy: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: Subnet })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "Subnet",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async subnet(
    @common.Param() params: SubnetWhereUniqueInput
  ): Promise<Subnet | null> {
    const result = await this.service.subnet({
      where: params,
      select: {
        created: true,
        id: true,
        isActive: true,
        note: true,
        purpose: true,
        updated: true,
        updatedBy: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: Subnet })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "Subnet",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateSubnet(
    @common.Param() params: SubnetWhereUniqueInput,
    @common.Body() data: SubnetUpdateInput
  ): Promise<Subnet | null> {
    try {
      return await this.service.updateSubnet({
        where: params,
        data: data,
        select: {
          created: true,
          id: true,
          isActive: true,
          note: true,
          purpose: true,
          updated: true,
          updatedBy: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: Subnet })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "Subnet",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteSubnet(
    @common.Param() params: SubnetWhereUniqueInput
  ): Promise<Subnet | null> {
    try {
      return await this.service.deleteSubnet({
        where: params,
        select: {
          created: true,
          id: true,
          isActive: true,
          note: true,
          purpose: true,
          updated: true,
          updatedBy: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
