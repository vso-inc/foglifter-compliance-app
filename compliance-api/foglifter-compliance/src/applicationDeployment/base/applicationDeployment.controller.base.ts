/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { ApplicationDeploymentService } from "../applicationDeployment.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { ApplicationDeploymentCreateInput } from "./ApplicationDeploymentCreateInput";
import { ApplicationDeployment } from "./ApplicationDeployment";
import { ApplicationDeploymentFindManyArgs } from "./ApplicationDeploymentFindManyArgs";
import { ApplicationDeploymentWhereUniqueInput } from "./ApplicationDeploymentWhereUniqueInput";
import { ApplicationDeploymentUpdateInput } from "./ApplicationDeploymentUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class ApplicationDeploymentControllerBase {
  constructor(
    protected readonly service: ApplicationDeploymentService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: ApplicationDeployment })
  @nestAccessControl.UseRoles({
    resource: "ApplicationDeployment",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createApplicationDeployment(
    @common.Body() data: ApplicationDeploymentCreateInput
  ): Promise<ApplicationDeployment> {
    return await this.service.createApplicationDeployment({
      data: {
        ...data,

        application: data.application
          ? {
              connect: data.application,
            }
          : undefined,

        asset: data.asset
          ? {
              connect: data.asset,
            }
          : undefined,
      },
      select: {
        application: {
          select: {
            id: true,
          },
        },

        asset: {
          select: {
            id: true,
          },
        },

        created: true,
        deploymentId: true,
        deploymentName: true,
        deploymentNote: true,
        deploymentStatus: true,
        deploymentType: true,
        id: true,
        updated: true,
        updatedBy: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [ApplicationDeployment] })
  @ApiNestedQuery(ApplicationDeploymentFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "ApplicationDeployment",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async applicationDeployments(
    @common.Req() request: Request
  ): Promise<ApplicationDeployment[]> {
    const args = plainToClass(ApplicationDeploymentFindManyArgs, request.query);
    return this.service.applicationDeployments({
      ...args,
      select: {
        application: {
          select: {
            id: true,
          },
        },

        asset: {
          select: {
            id: true,
          },
        },

        created: true,
        deploymentId: true,
        deploymentName: true,
        deploymentNote: true,
        deploymentStatus: true,
        deploymentType: true,
        id: true,
        updated: true,
        updatedBy: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: ApplicationDeployment })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "ApplicationDeployment",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async applicationDeployment(
    @common.Param() params: ApplicationDeploymentWhereUniqueInput
  ): Promise<ApplicationDeployment | null> {
    const result = await this.service.applicationDeployment({
      where: params,
      select: {
        application: {
          select: {
            id: true,
          },
        },

        asset: {
          select: {
            id: true,
          },
        },

        created: true,
        deploymentId: true,
        deploymentName: true,
        deploymentNote: true,
        deploymentStatus: true,
        deploymentType: true,
        id: true,
        updated: true,
        updatedBy: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: ApplicationDeployment })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "ApplicationDeployment",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateApplicationDeployment(
    @common.Param() params: ApplicationDeploymentWhereUniqueInput,
    @common.Body() data: ApplicationDeploymentUpdateInput
  ): Promise<ApplicationDeployment | null> {
    try {
      return await this.service.updateApplicationDeployment({
        where: params,
        data: {
          ...data,

          application: data.application
            ? {
                connect: data.application,
              }
            : undefined,

          asset: data.asset
            ? {
                connect: data.asset,
              }
            : undefined,
        },
        select: {
          application: {
            select: {
              id: true,
            },
          },

          asset: {
            select: {
              id: true,
            },
          },

          created: true,
          deploymentId: true,
          deploymentName: true,
          deploymentNote: true,
          deploymentStatus: true,
          deploymentType: true,
          id: true,
          updated: true,
          updatedBy: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: ApplicationDeployment })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "ApplicationDeployment",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteApplicationDeployment(
    @common.Param() params: ApplicationDeploymentWhereUniqueInput
  ): Promise<ApplicationDeployment | null> {
    try {
      return await this.service.deleteApplicationDeployment({
        where: params,
        select: {
          application: {
            select: {
              id: true,
            },
          },

          asset: {
            select: {
              id: true,
            },
          },

          created: true,
          deploymentId: true,
          deploymentName: true,
          deploymentNote: true,
          deploymentStatus: true,
          deploymentType: true,
          id: true,
          updated: true,
          updatedBy: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
