/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { ApplicationContactService } from "../applicationContact.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { ApplicationContactCreateInput } from "./ApplicationContactCreateInput";
import { ApplicationContact } from "./ApplicationContact";
import { ApplicationContactFindManyArgs } from "./ApplicationContactFindManyArgs";
import { ApplicationContactWhereUniqueInput } from "./ApplicationContactWhereUniqueInput";
import { ApplicationContactUpdateInput } from "./ApplicationContactUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class ApplicationContactControllerBase {
  constructor(
    protected readonly service: ApplicationContactService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: ApplicationContact })
  @nestAccessControl.UseRoles({
    resource: "ApplicationContact",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createApplicationContact(
    @common.Body() data: ApplicationContactCreateInput
  ): Promise<ApplicationContact> {
    return await this.service.createApplicationContact({
      data: {
        ...data,

        application: {
          connect: data.application,
        },

        contact: {
          connect: data.contact,
        },
      },
      select: {
        application: {
          select: {
            id: true,
          },
        },

        applicationId: true,

        contact: {
          select: {
            id: true,
          },
        },

        created: true,
        id: true,
        roleEnds: true,
        roleStarts: true,
        roleType: true,
        updated: true,
        updatedBy: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [ApplicationContact] })
  @ApiNestedQuery(ApplicationContactFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "ApplicationContact",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async applicationContacts(
    @common.Req() request: Request
  ): Promise<ApplicationContact[]> {
    const args = plainToClass(ApplicationContactFindManyArgs, request.query);
    return this.service.applicationContacts({
      ...args,
      select: {
        application: {
          select: {
            id: true,
          },
        },

        applicationId: true,

        contact: {
          select: {
            id: true,
          },
        },

        created: true,
        id: true,
        roleEnds: true,
        roleStarts: true,
        roleType: true,
        updated: true,
        updatedBy: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: ApplicationContact })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "ApplicationContact",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async applicationContact(
    @common.Param() params: ApplicationContactWhereUniqueInput
  ): Promise<ApplicationContact | null> {
    const result = await this.service.applicationContact({
      where: params,
      select: {
        application: {
          select: {
            id: true,
          },
        },

        applicationId: true,

        contact: {
          select: {
            id: true,
          },
        },

        created: true,
        id: true,
        roleEnds: true,
        roleStarts: true,
        roleType: true,
        updated: true,
        updatedBy: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: ApplicationContact })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "ApplicationContact",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateApplicationContact(
    @common.Param() params: ApplicationContactWhereUniqueInput,
    @common.Body() data: ApplicationContactUpdateInput
  ): Promise<ApplicationContact | null> {
    try {
      return await this.service.updateApplicationContact({
        where: params,
        data: {
          ...data,

          application: {
            connect: data.application,
          },

          contact: {
            connect: data.contact,
          },
        },
        select: {
          application: {
            select: {
              id: true,
            },
          },

          applicationId: true,

          contact: {
            select: {
              id: true,
            },
          },

          created: true,
          id: true,
          roleEnds: true,
          roleStarts: true,
          roleType: true,
          updated: true,
          updatedBy: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: ApplicationContact })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "ApplicationContact",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteApplicationContact(
    @common.Param() params: ApplicationContactWhereUniqueInput
  ): Promise<ApplicationContact | null> {
    try {
      return await this.service.deleteApplicationContact({
        where: params,
        select: {
          application: {
            select: {
              id: true,
            },
          },

          applicationId: true,

          contact: {
            select: {
              id: true,
            },
          },

          created: true,
          id: true,
          roleEnds: true,
          roleStarts: true,
          roleType: true,
          updated: true,
          updatedBy: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
